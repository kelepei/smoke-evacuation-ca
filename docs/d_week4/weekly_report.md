# D 第四周工作进度

## 本周完成

1. 在统一网页的真实联调模式下新增实验结果包导出；
2. 将 A 地图、C 人群/关系和 YAML、B CA 的逐步输出保存为可下载 ZIP；
3. 从真实 CSV 日志生成基础指标、疏散曲线和人员占用热力图；
4. 完成仓库真实示例的端到端验证；
5. 新增自动测试，验证网页接口导出的 ZIP 包含日志、指标、图表和输入文件。
6. 补齐右侧“策略方案”可交互页面：可切换基准、静态标识强化和动态引导预设，并明确真实联调阶段不会越过 B 的接口改写移动决策；
7. 建立网页周度版本留档：保留第二周、第三周与第四周 HTML 快照，同时维持当前 `integrated_runtime.html` 作为最新运行入口。

## 验证结果

运行 `python -B -m unittest discover -q`：26 项测试全部通过。

真实 A+B+C 示例运行 12 步后：40 人中 10 人已撤离，结果包包含 520 条逐人逐步数据记录和 10 个结果文件。

## 当前问题

当前 B 未输出实际离开出口及部分扩展字段，C 的策略行为尚未注入 B 的移动决策；D 不会补造这些结果。

仓库真实示例必须经本地 D 服务打开；直接双击 HTML 时，浏览器无法启动 Python 的 B 元胞自动机。该限制已在页面中给出操作提示。

## 后续计划

1. 待 B 输出 `actual_exit` 后加入出口利用率；
2. 待 A 提供带烟源、多出口的真实地图后补烟雾/出口对比实验；
3. 待 B/C 提供风险、信息和策略状态后扩展指标与策略对比。
