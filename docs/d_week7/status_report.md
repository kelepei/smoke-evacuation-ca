# D 端第 7 周联调状态报告

## 任务书口径

任务书第 7 至 8 周要求全员集成 v0.1，完成无烟/有烟、无社会关系/有社会关系的对比实验，并形成演示视频和阶段报告。因此当前不能把整个第 7 至 8 周任务标记为完成。

## 已完成

- 统一前端可手工导入 A 的 JSON、CSV、PNG，并通过统一 Grid 绘制墙体、出口、障碍和自由区域。
- C 的人员文件、YAML 场景配置、人数和随机种子进入当前仿真初始化。
- B 通过正式 `run_one_step(c_step_data)` 逐步运行，D 不再调用旧 `step()` 作为正式入口。
- D Canvas 显示人员、出口和烟雾；支持开始、暂停、单步、重置和结果导出。
- 每步写入 `people_log.csv`，首次撤离写入 `event_log.csv`，并生成 metrics 和最终帧。
- 新增自动验收报告，检查移动、越墙、人员重叠、烟雾范围和日志一致性。
- 去除 B Matplotlib 与 D Canvas 的重复逐帧绘制，并停止每步写 PNG。135 x 116、40 人样例的后端平均单步由约 438 ms 降至约 70 至 100 ms。

## 实测结果

无烟仓库样例：

- 40 人全部移动并于第 158 步全部撤离。
- 未发现人员进入墙体或障碍。
- `people_log.csv` 共 6360 行，对应 159 个状态乘 40 人。
- `event_log.csv` 中 40 条首次撤离事件与最终撤离人数一致。
- 135 个步骤出现活动人员同元胞，首次发生在第 1 步，单步最多 8 个重叠元胞。

烟雾契约专测：

- `smoke_matrix[y][x]` 已按 B 约定读取。
- 烟雾从第 4 步起超过约定的 0 到 10，12 步时最大值达到 13952.328。
- D 保留原始值并报警，没有在可视化层伪造修正。

## 尚未完成

- B 需要修复多人同元胞的冲突处理。
- B 需要确认烟雾扩散稳定性、0 到 10 范围、墙体阻隔和衰减规则。
- C 的每步人群行为/信息传播/引导策略输出当前仍为空映射，尚不能做真实的有无社会关系对比。
- 当前仓库样例没有烟源，需由 A/B 提供正式有烟场景。
- PNG 当前二值解析主要得到墙体/自由区域，出口和障碍语义仍需 A 确认。
- 无烟/有烟和无关系/有关系的正式批量对比、演示视频及阶段报告尚未完成。

## 结论

D 端的导入、渲染、控制、日志、指标和验收工具已经形成可运行闭环，但项目 v0.1 还不能按任务书宣布整体验收通过。下一步应先由 B 修复状态契约问题、C 提供真实每步行为输入，再运行四组可复现实验并制作演示视频。
